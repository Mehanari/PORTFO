import { EmblaOptionsType } from 'embla-carousel';

const OPTIONS = { dragFree: true, loop: true }
const SLIDE_COUNT = 4
const SLIDES = Array.from(Array(SLIDE_COUNT).keys())

export {OPTIONS, SLIDE_COUNT, SLIDES};